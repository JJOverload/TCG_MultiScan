# EasyOCR Coding and Testing

**Intro**

Since Tessearct is slow for the real life photos, I'm trying out the recommeneded "easyocr" instead, which seems to be much faster or "not-so-clean" photos (aka real-life photos).

TODO:
Would need to find ways to adjust bbox merging algorithm for easyocr

-----------------------------------------

**Installation**

If making an venv, I recommend calling it .venv in order to make use of gitignore file, in order to ignore virtual environment:
- For Windows:
`python -m venv .venv`

<br>
Then:

`pip install easyocr`

-----------------------------------------